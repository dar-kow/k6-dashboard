# Backend Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000

# K6 Tests Configuration
# Ścieżka do katalogu k6-tests (relatywna do backend/)
K6_TESTS_DIR=../k6-tests

# Results Configuration  
# Ścieżka do katalogu z wynikami testów
RESULTS_DIR=../k6-tests/results

# Logging
LOG_LEVEL=debug

# Optional: Custom paths for different environments
# K6_TESTS_DIR=/absolute/path/to/k6-tests
# RESULTS_DIR=/absolute/path/to/results