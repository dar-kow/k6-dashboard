# =============================================================================
# K6 DASHBOARD CONFIGURATION
# =============================================================================

# Server Configuration
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost

# =============================================================================
# BASE DIRECTORIES CONFIGURATION
# =============================================================================

# Base directory where all test repositories will be stored
# Each repository will create its own subdirectory (k6-tests-api, k6-tests-mobile, etc.)
BASE_K6_TESTS_DIR=../k6-tests-repos

# Base directory where all test results will be stored
# Each repository will have its own results subdirectory
BASE_RESULTS_DIR=../results

# =============================================================================
# BASIC SYSTEM CONFIGURATION
# =============================================================================

# Basic system information (displayed in dashboard)
SYSTEM_NAME=Performance Testing Suite
SYSTEM_VERSION=v2.0.0
SYSTEM_DESCRIPTION=Multi-repository K6 performance testing dashboard

# Test Configuration Defaults
DEFAULT_TEST_PROFILE=LIGHT
MAX_CONCURRENT_TESTS=3
TEST_TIMEOUT=300000

# Logging Level (debug, info, warn, error)
LOG_LEVEL=debug

# =============================================================================
# BACKWARD COMPATIBILITY (OPTIONAL)
# =============================================================================

# Default repository URL (will be added automatically as first repository)
# Leave empty if you want to add repositories manually through the UI
K6_TESTS_REPO_URL=https://github.com/dar-kow/k6-tests.git

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
# For Docker deployment, use these paths:
# BASE_K6_TESTS_DIR=/k6-tests-repos
# BASE_RESULTS_DIR=/results

# =============================================================================
# DIRECTORY STRUCTURE EXAMPLE
# =============================================================================
# 
# k6-tests-repos/
# ├── repositories.json (managed automatically)
# ├── k6-tests-api/
# │   ├── config/
# │   │   └── env.js (environment and token configuration)
# │   ├── tests/
# │   │   ├── login.js
# │   │   └── users.js
# │   └── results/
# ├── k6-tests-mobile/
# │   ├── config/
# │   │   └── env.js
# │   ├── tests/
# │   └── results/
# └── k6-tests-payment/
#     ├── config/
#     │   └── env.js
#     ├── tests/
#     └── results/
#
# Each repository's config/env.js contains:
# - HOSTS (PROD, DEV, STAGING URLs)
# - TOKENS (authentication tokens per environment)
# - LOAD_PROFILES (test load configurations)
# - ENVIRONMENT_INFO (display information)
#
# This approach allows each team/project to maintain their own:
# - Test repository
# - Environment configuration
# - Authentication tokens
# - Custom load profiles