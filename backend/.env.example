# Backend Environment Configuration Template
# Skopiuj ten plik jako .env i dostosuj ścieżki do swojego projektu

# Server Configuration
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000

# K6 Tests Configuration
# Ścieżka do katalogu k6-tests (relatywna do backend/ lub absolutna)
K6_TESTS_DIR=../k6-tests

# Results Configuration  
# Ścieżka do katalogu z wynikami testów
RESULTS_DIR=../k6-tests/results

# Logging Level (debug, info, warn, error)
LOG_LEVEL=debug

# Production Example:
# NODE_ENV=production
# K6_TESTS_DIR=/app/k6-tests
# RESULTS_DIR=/app/results
# LOG_LEVEL=info